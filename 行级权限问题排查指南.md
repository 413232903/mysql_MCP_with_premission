# 行级权限问题排查指南

## 问题分析

经过代码检查，我发现行级权限系统已经正确实现，但可能存在以下配置或使用问题导致权限未生效：

### 1. 配置问题（最常见）

**问题**：权限控制默认是关闭的，需要手动启用

**解决方案**：
在 `.env` 文件中添加以下配置：
```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表（逗号分隔）
PERMISSION_TABLES=表名1,表名2,表名3
```

### 2. 调用时未传入 user_id

**问题**：调用 MCP 工具时没有传入 `user_id` 参数

**解决方案**：
```python
# ❌ 错误：没有传入 user_id
result = await mysql_query(query="SELECT * FROM orders")

# ✅ 正确：传入 user_id
result = await mysql_query(
    query="SELECT * FROM orders",
    user_id="zhang_san"  # 必须传入用户ID
)
```

### 3. 查询的表不在权限表列表中

**问题**：查询的表名没有在 `PERMISSION_TABLES` 中配置

**解决方案**：
确保查询的表名在配置中，例如：
```env
PERMISSION_TABLES=orders,products,customers
```

### 4. 用户是超级管理员

**问题**：如果用户ID在 `SUPER_ADMIN_USERS` 列表中，会跳过权限检查

**解决方案**：
检查 `.env` 文件中的 `SUPER_ADMIN_USERS` 配置，确保测试用户不在列表中

### 5. SQL 不是 SELECT 查询

**问题**：行级权限只对 SELECT 查询生效，其他操作（INSERT、UPDATE、DELETE）不应用权限过滤

**解决方案**：
确认你的 SQL 是 SELECT 查询

## 快速诊断

### 方法1：运行诊断脚本

我已经创建了诊断脚本 `diagnose_permission.py`，运行它可以看到详细的配置信息：

```bash
python diagnose_permission.py
```

该脚本会检查：
- ✅ 权限控制是否启用
- ✅ 权限表是否配置
- ✅ 配置项是否正确
- ✅ SQL 解析是否正常

### 方法2：查看服务器日志

启动服务器后，查看日志输出：

**如果权限控制已正确启用，你会看到：**
```
INFO - ✅ 角色权限控制已启用: 权限表=['orders', 'products'], 权限字段=gssq
INFO - ✅ 已为用户 zhang_san 注入权限过滤条件
INFO - 📝 原始 SQL: SELECT * FROM orders WHERE status='pending'
INFO - 🔒 修改后 SQL: SELECT * FROM orders WHERE status='pending' AND (gssq IN ...)
```

**如果配置有问题，你会看到警告：**
```
WARNING - ⚠️ 未提供 user_id 参数，跳过权限控制。请在调用时传入 user_id 参数
WARNING - ⚠️ 角色权限控制已启用，但未配置权限表！
```

## 完整配置示例

在 `.env` 文件中添加以下完整配置：

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 需要权限过滤的表（逗号分隔，不区分大小写）
PERMISSION_TABLES=orders,products,customers,invoices

# 权限字段名称（默认 gssq）
PERMISSION_FIELD=gssq

# 用户角色表配置
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX

# 超级管理员用户列表（逗号分隔，这些用户会跳过权限检查）
SUPER_ADMIN_USERS=admin,system
```

## 代码改进

我已经对代码进行了以下改进：

1. **增强日志输出**：
   - 添加了更清晰的日志信息，包括 ✅、⚠️、ℹ️ 等标识
   - 当配置有问题时会显示警告信息
   - 显示原始 SQL 和修改后的 SQL，方便调试

2. **改进错误提示**：
   - 当未传入 `user_id` 时，会显示明确的警告信息
   - 当权限表未配置时，会提示如何配置

3. **创建诊断工具**：
   - `diagnose_permission.py` 脚本可以快速检查所有配置

## 验证步骤

1. **检查配置**：
   ```bash
   python diagnose_permission.py
   ```

2. **启动服务器**：
   ```bash
   python -m src.server
   ```

3. **查看启动日志**：
   确认看到 "✅ 角色权限控制已启用" 的日志

4. **测试查询**：
   ```python
   result = await mysql_query(
       query="SELECT * FROM orders",
       user_id="test_user"
   )
   ```

5. **查看执行日志**：
   确认看到 "✅ 已为用户 test_user 注入权限过滤条件" 的日志

## 数据库表结构要求

### 业务表（需要权限控制的表）

必须包含权限字段（默认 `gssq`）：
```sql
CREATE TABLE orders (
    id INT PRIMARY KEY,
    order_no VARCHAR(50),
    gssq VARCHAR(50),  -- 权限字段，存储权限标识
    -- 其他字段...
);
```

### 用户角色表（fr_user_role）

存储用户权限信息：
```sql
CREATE TABLE fr_user_role (
    id INT PRIMARY KEY,
    username VARCHAR(50),  -- 用户名
    extend1 VARCHAR(50),   -- 存储权限标识，格式如：RX001, RX002
    -- 其他字段...
);
```

**权限逻辑**：
- 如果 `extend1 = 'RX001'`，用户只能查询 `gssq = '001'` 的数据
- 系统会自动去除 `RX` 前缀，然后与 `gssq` 字段匹配

## 如果问题仍然存在

如果按照以上步骤配置后，权限仍然未生效，请：

1. 运行诊断脚本并查看输出
2. 检查服务器日志中的详细错误信息
3. 确认数据库表结构是否正确
4. 确认用户角色表中的数据是否正确
5. 提供详细的错误日志和配置信息，以便进一步排查

## 总结

行级权限功能已经正确实现，主要问题通常是：
1. **未启用权限控制**（`ENABLE_ROLE_PERMISSION=false`）
2. **未配置权限表**（`PERMISSION_TABLES` 为空）
3. **调用时未传入 user_id**
4. **查询的表不在权限表列表中**

按照本指南的步骤进行配置和排查，应该能够解决问题。

