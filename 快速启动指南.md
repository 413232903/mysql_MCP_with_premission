# 快速启动指南 - SSE 路由可配置

## ✅ 修复状态

代码已改成读取 `.env` 中的 `SSE_PATH` 与 `MOUNT_PATH`，默认监听 `/sse2`，如果你修改环境变量，服务会跟着更新。

## 🚀 启动服务器

### 方法1：正常启动（推荐）

```bash
python -m src.server
```

### 方法2：调试模式启动（显示详细配置信息）

```bash
python start_server_debug.py
```

启动后，你应该看到类似输出：

```
开始启动MySQL查询SSE服务器...
服务器监听在 127.0.0.1:3000/sse2  # 如果未修改 .env
```

## 🧪 测试访问

### 使用 curl 测试：

> 假设 `.env` 未修改，默认测试如下；如果你自定义了路径，请把 `/sse2` 换成新的后缀。

```bash
# 正确的路径 - 应该成功 ✅
curl http://127.0.0.1:3000/sse2

# 旧路径 - 应该失败 (404) ❌
curl http://127.0.0.1:3000/sse
```

### 使用浏览器测试：

访问：`http://127.0.0.1:3000/sse2`

## ⚠️ 重要提示

### 如果仍然无法访问 /sse2：

1. **确保服务器已完全重启**
   ```bash
   # 停止所有旧进程
   pkill -f "python.*server"
   
   # 清除缓存
   rm -rf src/__pycache__ src/*/__pycache__
   
   # 重新启动
   python -m src.server
   ```

2. **检查端口和主机**
   - 默认：`http://127.0.0.1:3000/sse2`
   - 如果修改了 `.env`，请使用其中的 `HOST`、`PORT`、`MOUNT_PATH`、`SSE_PATH`

3. **清除浏览器缓存**
   - 按 `Cmd + Shift + R` (Mac) 或 `Ctrl + Shift + R` (Windows) 强制刷新

4. **使用诊断脚本**
   ```bash
   ./diagnose_and_fix.sh
   ```

## 📋 配置信息

当前配置（来自 `.env`）：
- **SSE 路径**: `/sse2`
- **挂载路径**: `/`
- **完整 URL**: `http://127.0.0.1:3000/sse2`

## 🔧 自定义路径

如果需要修改为其他路径（如 `/api/mysql`）：

1. 打开 `.env` 或创建 `.env`：
   ```ini
   MOUNT_PATH=/api
   SSE_PATH=/mysql
   ```
2. 重启服务器，终端输出会显示新的完整地址，如 `http://127.0.0.1:3000/api/mysql`

## 📚 相关文档

- `SSE2_FIX_GUIDE.md` - 详细的修复说明
- `SOLUTION_SSE2.md` - 完整的解决方案
- `CLAUDE.md` - 项目技术文档

## 💡 快速命令参考

```bash
# 启动服务器
python -m src.server

# 调试模式启动
python start_server_debug.py

# 诊断问题
./diagnose_and_fix.sh

# 测试连接（根据你的 SSE_PATH 调整）
curl http://127.0.0.1:3000/sse2

# 停止服务器
pkill -f "python.*server"

# 清除缓存
rm -rf src/__pycache__ src/*/__pycache__
```

---

**问题？** 如果遇到任何问题，运行 `./diagnose_and_fix.sh` 进行自动诊断。

