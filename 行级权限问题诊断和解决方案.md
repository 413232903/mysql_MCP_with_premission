# 行级权限问题诊断和解决方案

## 一、问题分析

根据查询结果，员工 01201811074 查询到了多个省区的客户数据（湖南、京津冀、广东、河南、四川、重庆、云南、广西），但根据权限配置，该员工应该只能看到**湖北和湖南**的客户数据。

## 二、问题原因

经过代码检查和配置分析，发现以下问题：

### 1. ❌ 权限控制未启用

**当前状态**：`ENABLE_ROLE_PERMISSION=false`（在 example.env 中）

**影响**：即使配置了权限表，权限过滤也不会生效

### 2. ❌ 权限表未配置

**当前状态**：`PERMISSION_TABLES=`（空，未配置）

**影响**：系统不知道哪些表需要应用权限过滤

### 3. ✅ 权限字段配置正确

**当前状态**：`PERMISSION_FIELD=gssq`（正确，表中确实有 gssq 字段）

### 4. ✅ 权限值转换逻辑已实现

代码中已经实现了权限值格式转换：
- 去掉 `RX` 前缀
- 去掉 `Y` 前缀
- 去掉 `销区` 后缀
- 使用模糊匹配（LIKE）来匹配表中的值

**用户权限值**：
- `Y两湖RX销区` → 转换后：`两湖` → 匹配：`湖北`、`湖南`、`两湖`
- `Y湖北RX销区` → 转换后：`湖北` → 匹配：`湖北`
- `Y湖南RX销区` → 转换后：`湖南` → 匹配：`湖南`

## 三、解决方案

### 步骤1：创建或编辑 .env 文件

如果项目根目录没有 `.env` 文件，请复制 `example.env` 创建：

```bash
# Windows PowerShell
Copy-Item example.env .env

# Windows CMD
copy example.env .env

# Linux/Mac
cp example.env .env
```

### 步骤2：配置权限控制

编辑 `.env` 文件，找到以下配置项并修改：

```env
# 角色权限控制配置（行级权限）
# 是否启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 需要应用权限过滤的表列表（逗号分隔，不区分大小写）
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段名称（默认 gssq）
PERMISSION_FIELD=gssq

# 用户角色表配置（使用默认值即可）
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX
```

### 步骤3：重启服务器

修改配置后，需要重启 MCP 服务器使配置生效。

### 步骤4：验证权限是否生效

重新执行查询，应该只能看到湖北和湖南的客户数据：

```python
# 调用 MCP 工具时，确保传入 user_id 参数
result = await mysql_query(
    query="SELECT customer_name, gssq, over_credit_amount FROM cw_ai_customer_receivables_analysis WHERE year = 2025 AND month = 9 AND over_credit_amount > 0 ORDER BY over_credit_amount DESC LIMIT 10",
    user_id="01201811074"
)
```

**预期结果**：只返回 `gssq` 字段值为 `湖北` 或 `湖南` 的客户数据。

## 四、权限过滤逻辑说明

### 权限过滤条件

系统会自动在 SQL 查询中注入以下权限过滤条件：

```sql
WHERE (
    EXISTS (
        SELECT 1
        FROM fr_user_role ur
        WHERE ur.username = '01201811074'
        AND gssq LIKE CONCAT('%', TRIM(REPLACE(REPLACE(REPLACE(ur.extend1, 'RX', ''), 'Y', ''), '销区', '')), '%')
    )
    OR gssq IN (
        SELECT TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))
        FROM fr_user_role
        WHERE username = '01201811074'
        AND TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', '')) IS NOT NULL
        AND TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', '')) != ''
    )
    OR '01201811074' NOT IN (SELECT username FROM fr_user_role)
)
```

### 权限值转换示例

| 原始权限值 | 去掉 RX | 去掉 Y | 去掉 销区 | 最终匹配值 |
|-----------|---------|--------|-----------|-----------|
| Y两湖RX销区 | Y两湖销区 | 两湖销区 | 两湖 | 两湖、湖北、湖南 |
| Y湖北RX销区 | Y湖北销区 | 湖北销区 | 湖北 | 湖北 |
| Y湖南RX销区 | Y湖南销区 | 湖南销区 | 湖南 | 湖南 |

### 匹配逻辑

1. **模糊匹配（LIKE）**：`gssq LIKE '%两湖%'` 可以匹配 `湖北`、`湖南`、`两湖` 等
2. **精确匹配（IN）**：转换后的权限值直接匹配表中的值
3. **默认权限**：如果用户不在 `fr_user_role` 表中，可以查询所有数据

## 五、排查步骤

如果配置后权限仍然未生效，请按以下步骤排查：

### 1. 检查配置是否生效

运行诊断脚本：
```bash
python check_permission_issue.py
```

### 2. 检查服务器日志

查看服务器启动日志，应该看到：
```
✅ 角色权限控制已启用: 权限表=['cw_ai_customer_receivables_analysis'], 权限字段=gssq
```

### 3. 检查查询日志

执行查询时，日志中应该显示：
```
✅ 已为用户 01201811074 注入权限过滤条件
📝 原始 SQL: SELECT customer_name, gssq, over_credit_amount FROM cw_ai_customer_receivables_analysis ...
🔒 修改后 SQL: SELECT customer_name, gssq, over_credit_amount FROM cw_ai_customer_receivables_analysis ... AND (gssq LIKE ...)
```

### 4. 检查 user_id 参数

确保调用 MCP 工具时传入了 `user_id` 参数：
```python
# ✅ 正确
mysql_query(query="...", user_id="01201811074")

# ❌ 错误（没有传入 user_id）
mysql_query(query="...")
```

### 5. 检查超级管理员配置

如果用户ID在 `SUPER_ADMIN_USERS` 列表中，会跳过权限检查。确保 `.env` 文件中：
```env
SUPER_ADMIN_USERS=
```
（留空或确保 01201811074 不在列表中）

## 六、预期结果

配置完成后，员工 01201811074 查询 2025年9月超资信额排名前十的客户时，应该只返回：

| 排名 | 客户名称 | 省区 | 超资信额（元） |
|------|---------|------|---------------|
| 1 | 丰沃达医药物流湖南有限公司 | 湖南 | 1,382,932.00 |
| ... | （其他湖北或湖南的客户） | 湖北/湖南 | ... |

**不应该**返回其他省区（如京津冀、广东、河南、四川、重庆、云南、广西）的客户数据。

## 七、总结

**问题根源**：
1. 权限控制未启用（`ENABLE_ROLE_PERMISSION=false`）
2. 权限表未配置（`PERMISSION_TABLES=` 为空）

**解决方案**：
1. 设置 `ENABLE_ROLE_PERMISSION=true`
2. 设置 `PERMISSION_TABLES=cw_ai_customer_receivables_analysis`
3. 重启服务器
4. 调用时传入 `user_id` 参数

**权限过滤逻辑**：已正确实现，支持权限值格式转换和模糊匹配。

