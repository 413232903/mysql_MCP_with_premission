# 权限控制使用指南

## 概述

本指南介绍如何配置和使用MySQL MCP服务器的行级权限控制功能。权限控制基于数据库表中的权限字段（`gssq`），确保用户只能查询到其有权限访问的数据。

## 快速开始

### 1. 配置环境变量

#### 方式一：使用配置工具（推荐）

运行配置工具进行交互式配置：

```bash
python setup_permission.py
```

工具会自动检查配置并提示修复问题。

#### 方式二：手动配置

编辑 `.env` 文件（如果不存在，从 `example.env` 复制），添加以下配置：

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表（逗号分隔）
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段名称（表中实际字段名）
PERMISSION_FIELD=gssq

# 数据库名称
MYSQL_DATABASE=datawh
```

### 2. 验证配置

运行诊断工具检查配置是否正确：

```bash
python diagnose_permission.py
```

### 3. 测试权限控制

运行测试脚本验证权限控制是否正常工作：

```bash
python test_permission_control.py
```

## 使用方法

### 在代码中调用

#### 方式一：直接调用数据库操作函数

```python
from src.db.mysql_operations import get_db_connection, execute_query

async with get_db_connection() as connection:
    query = "SELECT * FROM cw_ai_customer_receivables_analysis WHERE year = 2025"
    # 传递 user_id 参数以应用权限控制
    results = await execute_query(connection, query, user_id='01201811074')
```

#### 方式二：通过MCP工具调用

```python
# 如果使用MCP工具，确保传递 user_id 参数
results = await mysql_query(
    query='SELECT * FROM cw_ai_customer_receivables_analysis WHERE year = 2025',
    user_id='01201811074'
)
```

### 查看示例

运行示例脚本查看权限控制的效果：

```bash
python query_with_permission.py
```

该脚本会：
1. 执行无权限控制的查询（显示全部数据）
2. 执行有权限控制的查询（仅显示权限范围内的数据）
3. 对比两种查询结果的差异

## 权限控制工作原理

### 权限规则

权限控制基于以下规则：

1. **权限值转换**：
   - 从用户角色表（`fr_user_role`）读取权限值
   - 去掉 `RX` 前缀
   - 去掉 `Y` 前缀（如果存在）
   - 去掉 `销区` 后缀（如果存在）
   - 使用模糊匹配（`LIKE`）匹配表中的权限字段值

2. **权限匹配**：
   - 如果用户在角色表中有记录，只能查询权限范围内的数据
   - 如果用户不在角色表中，可以查询所有数据（默认全部权限）
   - 超级管理员（配置在 `SUPER_ADMIN_USERS`）豁免权限检查

3. **SQL注入**：
   - 系统自动在 SELECT 查询的 WHERE 子句中注入权限过滤条件
   - 只对配置在 `PERMISSION_TABLES` 中的表应用权限过滤
   - 只对 SELECT 查询应用权限过滤

### 示例：权限值转换

**用户角色表中的权限值**：
- `Y两湖RX销区` → 转换后：`两湖`
- `Y湖北RX销区` → 转换后：`湖北`
- `Y湖南RX销区` → 转换后：`湖南`

**表中的权限字段值**：
- `湖北`
- `湖南`
- `两湖`

**匹配逻辑**：
- `两湖` 可以匹配 `湖北`、`湖南`、`两湖`（使用 `LIKE '%两湖%'`）
- `湖北` 可以匹配 `湖北`、`湖北省` 等
- `湖南` 可以匹配 `湖南`、`湖南省` 等

## 配置说明

### 环境变量配置项

| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| `ENABLE_ROLE_PERMISSION` | 是否启用权限控制 | `false` | 是 |
| `PERMISSION_TABLES` | 需要权限过滤的表（逗号分隔） | 空 | 是 |
| `PERMISSION_FIELD` | 权限字段名称 | `gssq` | 否 |
| `USER_ROLE_TABLE` | 用户角色表名 | `fr_user_role` | 否 |
| `USER_ROLE_USERNAME_FIELD` | 用户名字段 | `username` | 否 |
| `USER_ROLE_EXTEND_FIELD` | 权限存储字段 | `extend1` | 否 |
| `USER_ROLE_PREFIX` | 权限前缀 | `RX` | 否 |
| `SUPER_ADMIN_USERS` | 超级管理员列表（逗号分隔） | 空 | 否 |

### 配置示例

```env
# 启用权限控制
ENABLE_ROLE_PERMISSION=true

# 配置权限表
PERMISSION_TABLES=cw_ai_customer_receivables_analysis,orders,products

# 自定义权限字段
PERMISSION_FIELD=gssq

# 配置超级管理员
SUPER_ADMIN_USERS=admin,system
```

## 故障排查

### 权限控制未生效

1. **检查配置是否启用**
   ```bash
   python diagnose_permission.py
   ```

2. **检查调用时是否传递了 user_id**
   - 确保调用 `execute_query` 或 `mysql_query` 时传入了 `user_id` 参数

3. **检查用户是否在超级管理员列表中**
   - 如果用户在 `SUPER_ADMIN_USERS` 中，会跳过权限检查

4. **检查查询的表是否在权限表中**
   - 只有配置在 `PERMISSION_TABLES` 中的表才会应用权限过滤

5. **检查SQL是否为SELECT查询**
   - 只有 SELECT 查询会应用权限过滤，其他操作类型不会

6. **查看服务器日志**
   - 日志中会显示权限检查的详细过程
   - 查看是否有权限过滤条件被注入

### 常见问题

**Q: 查询返回空结果**

A: 可能原因：
- 用户没有权限访问任何数据
- 权限值格式与表字段值不匹配
- 权限配置不正确

**Q: 查询结果没有被过滤**

A: 可能原因：
- 权限控制未启用
- 未传递 `user_id` 参数
- 查询的表不在 `PERMISSION_TABLES` 中
- 用户是超级管理员

**Q: 权限值格式不匹配**

A: 系统会自动转换权限值格式：
- 去掉 `RX`、`Y` 前缀和 `销区` 后缀
- 使用模糊匹配（`LIKE`）进行匹配
- 如果仍然不匹配，请检查权限值转换逻辑

## 工具说明

### setup_permission.py

配置工具，用于检查和配置权限控制相关的环境变量。

```bash
# 交互式配置
python setup_permission.py

# 显示配置说明
python setup_permission.py --show-instructions

# 自动修复配置
python setup_permission.py --auto-fix
```

### diagnose_permission.py

诊断工具，用于检查权限控制配置是否正确。

```bash
python diagnose_permission.py
```

### test_permission_control.py

测试脚本，用于验证权限控制是否正常工作。

```bash
python test_permission_control.py
```

### query_with_permission.py

示例脚本，演示如何正确调用MCP工具并传递user_id参数。

```bash
python query_with_permission.py
```

## 注意事项

1. **重启服务器**：修改 `.env` 文件后，需要重启服务器使配置生效

2. **权限字段**：确保表中存在配置的权限字段（默认 `gssq`）

3. **用户角色表**：确保用户角色表（默认 `fr_user_role`）存在且包含用户权限数据

4. **性能影响**：权限过滤会增加查询复杂度，可能影响查询性能

5. **日志级别**：建议将日志级别设置为 `INFO` 或 `DEBUG` 以便查看权限检查过程

## 技术支持

如果遇到问题，请：

1. 运行诊断工具检查配置
2. 查看服务器日志中的权限相关信息
3. 运行测试脚本验证功能
4. 检查用户权限数据是否正确

