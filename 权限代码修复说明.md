# 行级权限控制代码修复说明

## 修复的问题

### 问题1：权限值格式转换不完整 ✅ 已修复

**原问题：**
- 权限值格式：`Y两湖RX销区`、`Y湖北RX销区`、`Y湖南RX销区`
- 原代码只去掉 `RX` 前缀，结果是：`Y两湖销区`、`Y湖北销区`、`Y湖南销区`
- 表中的实际值：`湖北`、`湖南`、`两湖`、`湖南省`
- **不匹配！**

**修复方案：**
1. 去掉 `RX` 前缀
2. 去掉 `Y` 前缀（如果存在）
3. 去掉 `销区` 后缀（如果存在）
4. 使用 `LIKE CONCAT('%', 转换后的值, '%')` 进行模糊匹配

**修复后的逻辑：**
```sql
-- 权限值转换：Y两湖RX销区 -> 两湖
TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))

-- 匹配条件：sales_region LIKE '%两湖%'
-- 这样可以匹配：湖北、湖南、两湖、湖南省 等
```

### 问题2：权限字段配置 ✅ 已说明

**问题：**
- 默认权限字段是 `gssq`，但表中没有这个字段
- 实际权限字段应该是 `sales_region`

**解决方案：**
需要在 `.env` 文件中配置：
```env
PERMISSION_FIELD=sales_region
```

### 问题3：权限值映射逻辑 ✅ 已修复

**原问题：**
- `Y两湖销区` 需要匹配 `湖北` 或 `湖南` 或 `两湖`
- 原代码无法处理这种映射关系

**修复方案：**
使用模糊匹配 `LIKE '%值%'`，可以匹配包含该值的所有记录：
- `两湖` 可以匹配 `湖北`、`湖南`、`两湖`、`湖南省` 等
- `湖北` 可以匹配 `湖北`、`湖北省` 等
- `湖南` 可以匹配 `湖南`、`湖南省` 等

## 修复后的代码逻辑

### 权限条件构建

```python
def _build_permission_condition(self, user_id: str) -> str:
    # 权限值转换：Y两湖RX销区 -> 两湖
    permission_value_transform = (
        f"TRIM(REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', ''))"
    )
    
    # 使用模糊匹配
    condition = f"""(
    {self.permission_field} LIKE CONCAT('%', {permission_value_transform}, '%')
    OR {self.permission_field} IN (
        SELECT {permission_value_transform}
        FROM {self.user_role_table}
        WHERE {self.username_field} = '{safe_user_id}'
    )
    OR '{safe_user_id}' NOT IN (
        SELECT {self.username_field} FROM {self.user_role_table}
    )
)"""
```

### 生成的 SQL 示例

**原始 SQL：**
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
ORDER BY over_credit_amount DESC 
LIMIT 10
```

**注入权限条件后：**
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
AND (
    sales_region LIKE CONCAT('%', TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', '')), '%')
    OR sales_region IN (
        SELECT TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))
        FROM fr_user_role
        WHERE username = '01201811074'
    )
    OR '01201811074' NOT IN (SELECT username FROM fr_user_role)
)
ORDER BY over_credit_amount DESC 
LIMIT 10
```

## 测试验证

### 测试用例1：权限值格式转换

**输入：**
- 权限值：`Y两湖RX销区`
- 转换后：`两湖`
- 表字段值：`湖北`、`湖南`、`两湖`

**预期结果：**
- `sales_region LIKE '%两湖%'` 应该匹配 `湖北`、`湖南`、`两湖` ✅

### 测试用例2：权限值格式转换

**输入：**
- 权限值：`Y湖北RX销区`
- 转换后：`湖北`
- 表字段值：`湖北`、`湖北省`

**预期结果：**
- `sales_region LIKE '%湖北%'` 应该匹配 `湖北`、`湖北省` ✅

### 测试用例3：权限值格式转换

**输入：**
- 权限值：`Y湖南RX销区`
- 转换后：`湖南`
- 表字段值：`湖南`、`湖南省`

**预期结果：**
- `sales_region LIKE '%湖南%'` 应该匹配 `湖南`、`湖南省` ✅

## 配置要求

### 必须配置

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段改为 sales_region（因为表中没有 gssq 字段）
PERMISSION_FIELD=sales_region
```

### 可选配置（使用默认值即可）

```env
# 用户角色表配置
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX
```

## 注意事项

1. **性能影响**：使用 `LIKE '%值%'` 模糊匹配可能会影响查询性能，如果权限值很多，建议：
   - 优化权限值格式，使其与表字段值更匹配
   - 或者使用精确匹配 + 映射表的方式

2. **权限值格式**：如果可能，建议统一权限值格式：
   - 去掉 `Y` 前缀和 `销区` 后缀
   - 或者调整表字段值格式

3. **测试验证**：修复后需要测试：
   - 权限值格式转换是否正确
   - 权限过滤是否生效
   - 查询结果是否符合预期

