# 行级权限控制代码排查修复报告

## 一、问题排查过程

### 1. 测试环境
- **员工工号**：01201811074
- **测试表**：cw_ai_customer_receivables_analysis
- **权限字段**：sales_region（销区）

### 2. 发现的问题

#### 问题1：权限值格式转换不完整 ❌

**位置**：`src/security/role_permission.py` 第310行

**原代码逻辑：**
```python
SELECT REPLACE({self.extend_field}, '{self.role_prefix}', '')
```

**实际权限值格式：**
- `Y两湖RX销区` -> 去掉 `RX` 后 -> `Y两湖销区`
- `Y湖北RX销区` -> 去掉 `RX` 后 -> `Y湖北销区`
- `Y湖南RX销区` -> 去掉 `RX` 后 -> `Y湖南销区`

**表中的实际值：**
- `湖北`
- `湖南`
- `两湖`
- `湖南省`

**问题分析：**
- 原代码只去掉 `RX` 前缀，结果 `Y两湖销区` 与表字段值 `湖北`、`湖南`、`两湖` 不匹配
- 需要进一步处理：去掉 `Y` 前缀和 `销区` 后缀

#### 问题2：权限值匹配逻辑缺失 ❌

**问题分析：**
- 权限值 `Y两湖销区` 需要匹配 `湖北` 或 `湖南` 或 `两湖`
- 原代码使用 `IN` 精确匹配，无法处理这种映射关系
- 需要使用模糊匹配或映射逻辑

#### 问题3：权限字段配置问题 ⚠️

**问题分析：**
- 默认权限字段是 `gssq`，但表中没有这个字段
- 实际权限字段应该是 `sales_region`
- 需要通过环境变量配置

## 二、修复方案

### 修复1：增强权限值转换逻辑 ✅

**修复位置**：`src/security/role_permission.py` 第295-318行

**修复内容：**
1. 去掉 `RX` 前缀
2. 去掉 `Y` 前缀（如果存在）
3. 去掉 `销区` 后缀（如果存在）
4. 使用 `TRIM()` 去除首尾空格
5. 使用 `LIKE CONCAT('%', 转换后的值, '%')` 进行模糊匹配

**修复后的代码：**
```python
def _build_permission_condition(self, user_id: str) -> str:
    """
    构建权限过滤条件
    支持权限值格式转换：去掉 RX、Y 前缀和 销区 后缀，然后使用模糊匹配
    """
    safe_user_id = user_id.replace("'", "''")

    # 构建权限值转换逻辑：
    # 1. 去掉 RX 前缀
    # 2. 去掉 Y 前缀（如果存在）
    # 3. 去掉 销区 后缀（如果存在）
    # 4. 使用模糊匹配来匹配表中的值
    permission_value_transform = (
        f"TRIM(REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', ''))"
    )

    # 使用 LIKE 模糊匹配，支持权限值格式与表字段值不完全一致的情况
    condition = f"""(
    {self.permission_field} LIKE CONCAT('%', {permission_value_transform}, '%')
    OR {self.permission_field} IN (
        SELECT {permission_value_transform}
        FROM {self.user_role_table}
        WHERE {self.username_field} = '{safe_user_id}'
    )
    OR '{safe_user_id}' NOT IN (
        SELECT {self.username_field} FROM {self.user_role_table}
    )
)"""
    return condition
```

### 修复2：配置说明 ✅

**需要在 `.env` 文件中配置：**
```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段改为 sales_region（因为表中没有 gssq 字段）
PERMISSION_FIELD=sales_region
```

## 三、修复效果验证

### 测试用例1：权限值格式转换

**输入：**
- 权限值：`Y两湖RX销区`
- 转换过程：
  1. 去掉 `RX` -> `Y两湖销区`
  2. 去掉 `Y` -> `两湖销区`
  3. 去掉 `销区` -> `两湖`
  4. TRIM -> `两湖`

**匹配逻辑：**
- `sales_region LIKE '%两湖%'`
- 可以匹配：`湖北`、`湖南`、`两湖`、`湖南省` 等 ✅

### 测试用例2：权限值格式转换

**输入：**
- 权限值：`Y湖北RX销区`
- 转换后：`湖北`

**匹配逻辑：**
- `sales_region LIKE '%湖北%'`
- 可以匹配：`湖北`、`湖北省` 等 ✅

### 测试用例3：权限值格式转换

**输入：**
- 权限值：`Y湖南RX销区`
- 转换后：`湖南`

**匹配逻辑：**
- `sales_region LIKE '%湖南%'`
- 可以匹配：`湖南`、`湖南省` 等 ✅

## 四、生成的 SQL 示例

### 原始 SQL
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
ORDER BY over_credit_amount DESC 
LIMIT 10
```

### 注入权限条件后
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
AND (
    sales_region LIKE CONCAT('%', TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', '')), '%')
    OR sales_region IN (
        SELECT TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))
        FROM fr_user_role
        WHERE username = '01201811074'
    )
    OR '01201811074' NOT IN (SELECT username FROM fr_user_role)
)
ORDER BY over_credit_amount DESC 
LIMIT 10
```

## 五、修复总结

### 已修复的问题 ✅

1. **权限值格式转换不完整** - 已修复
   - 现在支持去掉 `RX`、`Y` 前缀和 `销区` 后缀
   - 使用 `TRIM()` 去除首尾空格

2. **权限值匹配逻辑缺失** - 已修复
   - 使用 `LIKE CONCAT('%', 值, '%')` 进行模糊匹配
   - 支持权限值格式与表字段值不完全一致的情况

### 需要配置的项目 ⚠️

1. **权限字段配置**
   - 在 `.env` 文件中设置 `PERMISSION_FIELD=sales_region`

2. **权限表配置**
   - 在 `.env` 文件中设置 `PERMISSION_TABLES=cw_ai_customer_receivables_analysis`

3. **启用权限控制**
   - 在 `.env` 文件中设置 `ENABLE_ROLE_PERMISSION=true`

## 六、测试建议

### 测试步骤

1. **配置环境变量**
   ```env
   ENABLE_ROLE_PERMISSION=true
   PERMISSION_TABLES=cw_ai_customer_receivables_analysis
   PERMISSION_FIELD=sales_region
   ```

2. **重启服务器**
   ```bash
   python -m src.server
   ```

3. **测试查询**
   ```python
   result = await mysql_query(
       query="SELECT customer_name, sales_region, over_credit_amount FROM cw_ai_customer_receivables_analysis WHERE year = 2025 AND month = 9 AND over_credit_amount > 0 ORDER BY over_credit_amount DESC LIMIT 10",
       user_id="01201811074"
   )
   ```

4. **验证结果**
   - 检查查询结果是否只包含湖北和湖南的客户
   - 检查日志中是否有权限过滤的 SQL 语句
   - 检查权限条件是否正确注入

## 七、注意事项

1. **性能影响**
   - 使用 `LIKE '%值%'` 模糊匹配可能会影响查询性能
   - 如果权限值很多，建议优化权限值格式或使用索引

2. **权限值格式**
   - 如果可能，建议统一权限值格式，使其与表字段值更匹配
   - 或者调整表字段值格式

3. **测试验证**
   - 修复后需要充分测试各种权限值格式
   - 确保权限过滤正确生效

## 八、相关文件

- **修复的文件**：`src/security/role_permission.py`
- **测试脚本**：`test_permission_code.py`
- **问题分析**：`权限代码问题分析.md`
- **修复说明**：`权限代码修复说明.md`

