# 行级权限控制代码问题分析

## 发现的问题

### 问题1：权限值格式转换不完整

**当前代码逻辑（第310行）：**
```python
SELECT REPLACE({self.extend_field}, '{self.role_prefix}', '')
```

**实际权限值：**
- `Y两湖RX销区` -> 去掉 `RX` 后 -> `Y两湖销区`
- `Y湖北RX销区` -> 去掉 `RX` 后 -> `Y湖北销区`
- `Y湖南RX销区` -> 去掉 `RX` 后 -> `Y湖南销区`

**表中的实际值：**
- `湖北`
- `湖南`
- `两湖`
- `湖南省`

**问题：**
- 权限值格式 `Y两湖销区` 与表字段值 `湖北`、`湖南`、`两湖` 不匹配
- 需要进一步处理：去掉 `Y` 前缀和 `销区` 后缀

### 问题2：权限字段配置问题

**当前配置：**
- 默认权限字段：`gssq`
- 实际表字段：`sales_region`

**问题：**
- 如果使用默认配置，会查询不存在的 `gssq` 字段
- 需要通过环境变量配置为 `sales_region`

### 问题3：权限值映射逻辑缺失

**需要处理的映射：**
- `Y两湖销区` -> 需要匹配 `湖北` 或 `湖南` 或 `两湖`
- `Y湖北销区` -> 需要匹配 `湖北`
- `Y湖南销区` -> 需要匹配 `湖南` 或 `湖南省`

**当前代码问题：**
- 没有处理这种复杂的映射关系
- 只是简单的字符串替换

## 修复方案

### 方案1：增强权限值转换逻辑（推荐）

在 `_build_permission_condition` 方法中，添加更复杂的权限值转换逻辑：

```python
def _build_permission_condition(self, user_id: str) -> str:
    # 构建权限值转换逻辑
    # 1. 去掉 RX 前缀
    # 2. 去掉 Y 前缀（如果存在）
    # 3. 去掉 销区 后缀（如果存在）
    # 4. 处理特殊映射（如 两湖 -> 湖北,湖南）
    
    condition = f"""( {self.permission_field} IN (
        SELECT 
            CASE 
                WHEN REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', '') = '两湖' 
                THEN '湖北'
                WHEN REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', '') = '两湖' 
                THEN '湖南'
                ELSE REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', '')
            END
        FROM {self.user_role_table}
        WHERE {self.username_field} = '{safe_user_id}'
    ) OR {self.permission_field} LIKE CONCAT('%', REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', ''), '%')
    OR '{safe_user_id}' NOT IN (SELECT {self.username_field} FROM {self.user_role_table}) )"""
```

### 方案2：使用模糊匹配（简单但性能较差）

```python
def _build_permission_condition(self, user_id: str) -> str:
    condition = f"""( {self.permission_field} LIKE CONCAT('%', REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', ''), '%')
    OR '{safe_user_id}' NOT IN (SELECT {self.username_field} FROM {self.user_role_table}) )"""
```

### 方案3：在应用层处理（最灵活）

在查询前，先从 `fr_user_role` 表获取权限值，然后在应用层转换为匹配的销区值，再构建 SQL 条件。

## 推荐修复方案

**方案2（模糊匹配）** 是最简单且实用的方案，因为：
1. 实现简单
2. 可以处理各种格式的权限值
3. 性能影响较小（如果权限值不多）

**修复后的代码：**
```python
def _build_permission_condition(self, user_id: str) -> str:
    """
    构建权限过滤条件
    支持权限值格式转换：去掉 RX、Y 前缀和 销区 后缀，然后使用模糊匹配
    """
    safe_user_id = user_id.replace("'", "''")
    
    # 构建权限值转换：去掉 RX 前缀、Y 前缀、销区后缀
    permission_value_transform = f"REPLACE(REPLACE(REPLACE({self.extend_field}, '{self.role_prefix}', ''), 'Y', ''), '销区', '')"
    
    condition = f"""( {self.permission_field} LIKE CONCAT('%', {permission_value_transform}, '%')
    OR {self.permission_field} IN (
        SELECT {permission_value_transform}
        FROM {self.user_role_table}
        WHERE {self.username_field} = '{safe_user_id}'
    )
    OR '{safe_user_id}' NOT IN (
        SELECT {self.username_field} FROM {self.user_role_table}
    ) )"""
    
    return condition
```

