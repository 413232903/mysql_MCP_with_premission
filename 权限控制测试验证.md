# 权限控制代码测试验证报告

## 一、测试环境

- **员工工号**：01201811074
- **测试表**：cw_ai_customer_receivables_analysis
- **权限字段**：sales_region（销区）

## 二、权限值转换测试

### 测试用例1：权限值格式转换

**原始权限值：**
- `Y两湖RX销区`
- `Y湖北RX销区`
- `Y湖南RX销区`
- `Y湖北销区`
- `Y湖南销区`
- `Y湖北OTC销区`

**转换逻辑：**
```sql
TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))
```

**转换后结果：**
- `Y两湖RX销区` -> `两湖`
- `Y湖北RX销区` -> `湖北`
- `Y湖南RX销区` -> `湖南`
- `Y湖北销区` -> `湖北`
- `Y湖南销区` -> `湖南`
- `Y湖北OTC销区` -> `湖北OTC`

## 三、权限匹配测试

### 匹配逻辑

**生成的 SQL 条件：**
```sql
sales_region LIKE CONCAT('%', 转换后的值, '%')
```

**匹配示例：**

1. **权限值：`两湖`**
   - `sales_region LIKE '%两湖%'`
   - 可以匹配：`湖北`、`湖南`、`两湖`、`湖南省` 等 ✅

2. **权限值：`湖北`**
   - `sales_region LIKE '%湖北%'`
   - 可以匹配：`湖北`、`湖北省` 等 ✅

3. **权限值：`湖南`**
   - `sales_region LIKE '%湖南%'`
   - 可以匹配：`湖南`、`湖南省` 等 ✅

## 四、完整 SQL 测试

### 原始 SQL
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
ORDER BY over_credit_amount DESC 
LIMIT 10
```

### 注入权限条件后的 SQL
```sql
SELECT customer_name, sales_region, over_credit_amount 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 AND month = 9 
AND over_credit_amount > 0 
AND (
    sales_region LIKE CONCAT('%', TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', '')), '%')
    OR sales_region IN (
        SELECT TRIM(REPLACE(REPLACE(REPLACE(extend1, 'RX', ''), 'Y', ''), '销区', ''))
        FROM fr_user_role
        WHERE username = '01201811074'
    )
    OR '01201811074' NOT IN (SELECT username FROM fr_user_role)
)
ORDER BY over_credit_amount DESC 
LIMIT 10
```

## 五、预期结果验证

### 员工 01201811074 的权限范围

根据 `fr_user_role` 表中的数据：
- `Y两湖RX销区` -> `两湖` -> 匹配 `湖北`、`湖南`、`两湖`
- `Y湖北RX销区` -> `湖北` -> 匹配 `湖北`、`湖北省`
- `Y湖南RX销区` -> `湖南` -> 匹配 `湖南`、`湖南省`
- `Y湖北销区` -> `湖北` -> 匹配 `湖北`、`湖北省`
- `Y湖南销区` -> `湖南` -> 匹配 `湖南`、`湖南省`
- `Y湖北OTC销区` -> `湖北OTC` -> 匹配包含 `湖北OTC` 的值

### 预期查询结果

查询结果应该只包含：
- `sales_region` 为 `湖北` 的客户
- `sales_region` 为 `湖南` 的客户
- `sales_region` 为 `湖南省` 的客户
- `sales_region` 包含 `湖北` 的客户
- `sales_region` 包含 `湖南` 的客户

## 六、代码修复验证

### 修复前的问题

1. ❌ 只去掉 `RX` 前缀，结果 `Y两湖销区` 与表字段值不匹配
2. ❌ 使用 `IN` 精确匹配，无法处理映射关系

### 修复后的改进

1. ✅ 支持去掉 `RX`、`Y` 前缀和 `销区` 后缀
2. ✅ 使用 `LIKE CONCAT('%', 值, '%')` 进行模糊匹配
3. ✅ 支持权限值格式与表字段值不完全一致的情况

## 七、配置要求

### 必须配置

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段改为 sales_region
PERMISSION_FIELD=sales_region
```

### 可选配置（使用默认值）

```env
# 用户角色表配置
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX
```

## 八、测试步骤

1. **配置环境变量**
   - 在 `.env` 文件中添加上述配置

2. **重启服务器**
   ```bash
   python -m src.server
   ```

3. **执行测试查询**
   ```python
   result = await mysql_query(
       query="SELECT customer_name, sales_region, over_credit_amount FROM cw_ai_customer_receivables_analysis WHERE year = 2025 AND month = 9 AND over_credit_amount > 0 ORDER BY over_credit_amount DESC LIMIT 10",
       user_id="01201811074"
   )
   ```

4. **验证结果**
   - 检查查询结果是否只包含湖北和湖南的客户
   - 检查日志中是否有权限过滤的 SQL 语句
   - 检查权限条件是否正确注入

## 九、测试结论

### 代码修复验证 ✅

1. **权限值转换逻辑** - ✅ 已修复
   - 支持去掉 `RX`、`Y` 前缀和 `销区` 后缀
   - 使用 `TRIM()` 去除首尾空格

2. **权限匹配逻辑** - ✅ 已修复
   - 使用 `LIKE CONCAT('%', 值, '%')` 进行模糊匹配
   - 支持权限值格式与表字段值不完全一致的情况

3. **SQL 注入逻辑** - ✅ 正常
   - 权限条件正确注入到 WHERE 子句
   - 支持已有 WHERE 子句的情况

### 需要配置验证 ⚠️

1. **环境变量配置** - 需要确认
   - `ENABLE_ROLE_PERMISSION=true`
   - `PERMISSION_TABLES=cw_ai_customer_receivables_analysis`
   - `PERMISSION_FIELD=sales_region`

2. **实际查询测试** - 需要执行
   - 使用 `user_id="01201811074"` 进行查询
   - 验证查询结果是否符合预期

## 十、下一步操作

1. ✅ 代码已修复
2. ⚠️ 需要配置环境变量
3. ⚠️ 需要重启服务器
4. ⚠️ 需要执行实际查询测试
5. ⚠️ 需要验证查询结果

