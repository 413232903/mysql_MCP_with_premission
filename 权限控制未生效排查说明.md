# 权限控制未生效排查说明

## 问题描述

从日志来看，虽然传递了 `user_id: '01201811074'`，但是权限控制没有生效，查询执行时没有看到任何权限相关的日志输出。

## 问题原因分析

根据代码分析，权限控制未生效的可能原因有：

1. **权限控制未启用**：`ENABLE_ROLE_PERMISSION=false`（默认值）
2. **权限表未配置**：`PERMISSION_TABLES` 环境变量为空或未包含查询的表
3. **查询的表不在权限表中**：查询的表 `cw_ai_customer_receivables_analysis` 没有配置在 `PERMISSION_TABLES` 中
4. **用户是超级管理员**：`user_id` 在 `SUPER_ADMIN_USERS` 列表中
5. **日志级别问题**：之前的关键日志使用 DEBUG 级别，可能看不到

## 已修复的问题

### 1. 增强日志输出

我已经将权限检查的关键日志从 DEBUG 级别提升到 INFO 级别，确保能看到：
- 权限检查开始时的详细信息
- 权限控制启用状态
- 配置的权限表列表
- 解析到的表名
- 权限未应用的原因

### 2. 添加数据库操作层日志

在 `mysql_operations.py` 中添加了权限过滤过程的日志，可以看到：
- 权限过滤开始
- SQL 是否被权限过滤修改

## 排查步骤

### 步骤 1：运行排查脚本

运行我创建的排查脚本：

```bash
python check_permission_issue_now.py
```

这个脚本会自动检查：
- 权限控制是否启用
- 权限表是否配置
- 查询的表是否在权限表中
- 用户是否是超级管理员
- 权限管理器是否正常工作

### 步骤 2：检查环境变量配置

确保 `.env` 文件中包含以下配置：

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表（必须包含查询的表）
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 其他可选配置（使用默认值即可）
PERMISSION_FIELD=gssq
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX

# 超级管理员列表（如果该用户不应该跳过权限检查，不要添加）
# SUPER_ADMIN_USERS=admin,system
```

### 步骤 3：重启服务器

修改 `.env` 文件后，**必须重启服务器**才能使配置生效。

### 步骤 4：查看服务器日志

重启后，再次执行查询，应该能看到以下日志：

```
INFO - 🔍 开始权限检查 - user_id: 01201811074, SQL: SELECT customer_name...
INFO - 🔍 权限控制启用状态: True
INFO - 🔍 配置的权限表: ['cw_ai_customer_receivables_analysis']
INFO - 🔍 解析到的表名: ['cw_ai_customer_receivables_analysis']
INFO - ✅ 权限检查通过，将应用权限过滤 - user_id: 01201811074, 涉及表: ['cw_ai_customer_receivables_analysis']
INFO - ✅ 已为用户 01201811074 注入权限过滤条件
INFO - 📝 原始 SQL: SELECT customer_name...
INFO - 🔒 修改后 SQL: SELECT customer_name... AND (gssq IN ...)
```

如果看到以下日志，说明权限未应用：

```
INFO - ⚠️ 查询不涉及权限控制表，跳过权限控制。查询的表: [...], 配置的权限表: [...]
```

或

```
INFO - ❌ 权限控制未启用，跳过权限过滤。请在环境变量中设置 ENABLE_ROLE_PERMISSION=true
```

## 常见问题

### Q1: 为什么看不到权限相关的日志？

**A**: 之前的关键日志使用 DEBUG 级别，现在已修复为 INFO 级别。如果还是看不到，请检查：
1. 日志级别配置（`LOG_LEVEL` 环境变量）
2. 服务器是否已重启

### Q2: 权限表配置了，但还是不生效？

**A**: 请检查：
1. 表名大小写是否匹配（系统会自动转换为小写比较）
2. 表名是否完全一致（包括下划线、连字符等）
3. 是否重启了服务器

### Q3: 用户是超级管理员怎么办？

**A**: 如果用户 `01201811074` 不应该跳过权限检查，请确保它不在 `SUPER_ADMIN_USERS` 列表中。

### Q4: 如何确认权限过滤是否生效？

**A**: 查看日志中的 "修改后 SQL"，应该能看到添加了权限过滤条件：
```sql
AND (
    EXISTS (
        SELECT 1
        FROM fr_user_role ur
        WHERE ur.username = '01201811074'
        AND gssq LIKE CONCAT('%', TRIM(...), '%')
    )
    OR ...
)
```

## 修改的文件

1. `src/security/role_permission.py` - 增强日志输出
2. `src/db/mysql_operations.py` - 添加权限过滤日志
3. `check_permission_issue_now.py` - 新增排查脚本

## 下一步操作

1. 运行 `python check_permission_issue_now.py` 查看详细诊断信息
2. 根据诊断结果修改 `.env` 文件
3. 重启服务器
4. 再次执行查询，查看日志确认权限控制是否生效

