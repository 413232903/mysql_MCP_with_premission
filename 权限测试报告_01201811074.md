# 员工工号 01201811074 行级权限测试报告

## 一、表结构分析

### 1. 客户应收账款分析表 (cw_ai_customer_receivables_analysis)

**主要字段：**
- `customer_name`: 客户名称 (varchar(255))
- `sales_region`: 销区 (varchar(100)) - **这是权限字段**
- `over_credit_amount`: 超资信额 (decimal(20,2))
- `year`: 年份 (int)
- `month`: 月份 (int)
- `credit_limit`: 信用额度 (decimal(20,2))
- `current_receivable`: 当前应收账款 (decimal(20,2))
- 其他字段：应收账款相关指标

**重要发现：**
- ❌ 表中**没有 `gssq` 字段**（默认权限字段）
- ✅ 表中有 `sales_region` 字段（销区字段）

### 2. 用户角色表 (fr_user_role)

**主要字段：**
- `USERNAME`: 用户名 (varchar(50))
- `EXTEND1`: 权限标识 (varchar(2000)) - 存储权限信息
- `FULLNAME`: 姓名 (varchar(100))
- `ROLENAME`: 角色名 (varchar(255))

**员工 01201811074 的权限数据：**
```
USERNAME: 01201811074
EXTEND1 值（原始）: Y两湖RX销区, Y湖北RX销区, Y湖南RX销区, Y湖北销区, Y湖南销区, Y湖北OTC销区
去掉 RX 前缀后: Y两湖销区, Y湖北销区, Y湖南销区, Y湖北销区, Y湖南销区, Y湖北OTC销区
```

**重要发现：**
- 权限值格式：`Y两湖销区`、`Y湖北销区`、`Y湖南销区` 等
- 表中的 `sales_region` 值：`湖北`、`湖南`、`两湖` 等
- ⚠️ **权限值格式与表字段值不匹配**

## 二、取数逻辑说明

### 查询目标
获取当前年月（2025年9月）超资信额排名前十的客户名称

### SQL 查询逻辑
```sql
SELECT customer_name, sales_region, over_credit_amount, year, month 
FROM cw_ai_customer_receivables_analysis 
WHERE year = 2025 
  AND month = 9 
  AND over_credit_amount IS NOT NULL 
  AND over_credit_amount > 0 
ORDER BY over_credit_amount DESC 
LIMIT 10
```

### 逻辑步骤
1. **筛选条件**：
   - 年份 = 2025，月份 = 9（当前年月）
   - 超资信额不为空且大于0（只统计有超资信额的客户）

2. **排序**：
   - 按超资信额降序排列（金额最大的在前）

3. **限制结果**：
   - 只取前10条记录

## 三、权限控制配置问题

### 当前配置（默认）
- `PERMISSION_FIELD`: `gssq`（默认权限字段）
- `PERMISSION_TABLES`: 未配置（需要添加 `cw_ai_customer_receivables_analysis`）
- `ENABLE_ROLE_PERMISSION`: 需要确认是否启用

### 发现的问题

1. **权限字段不匹配**
   - 配置的权限字段是 `gssq`，但表中没有这个字段
   - 表中实际权限字段应该是 `sales_region`

2. **权限值格式不匹配**
   - 权限值：`Y两湖销区`、`Y湖北销区`、`Y湖南销区`
   - 表字段值：`湖北`、`湖南`、`两湖`
   - 需要调整权限值格式或表字段值

3. **权限表未配置**
   - `PERMISSION_TABLES` 需要包含 `cw_ai_customer_receivables_analysis`

## 四、测试结果

### 1. 无权限控制的查询结果（全部数据）

**2025年9月超资信额排名前十的客户：**

| 排名 | 客户名称 | 销区 | 超资信额（元） |
|------|---------|------|---------------|
| 1 | 丰沃达医药物流湖南有限公司 | 湖南 | 1,382,932.00 |
| 2 | 国药乐仁堂医药有限公司 | - | 1,222,280.50 |
| 3 | 佛山市大参林医药贸易有限公司 | - | 783,387.80 |
| 4 | 华润河南医药有限公司 | 河南 | 602,816.80 |
| 5 | 四川九州通医药有限公司 | 四川 | 575,043.40 |
| 6 | 重庆九州通医药有限公司 | 重庆 | 565,722.70 |
| 7 | 云南省医药有限公司 | 云南 | 563,623.10 |
| 8 | 四川本草堂药业有限公司 | 四川 | 439,114.20 |
| 9 | 国药控股广西有限公司 | 广西 | 390,907.10 |
| 10 | 华润科伦医药四川有限公司 | 四川 | 388,800.00 |

### 2. 按销区权限过滤后的预期结果

**员工 01201811074 的权限范围：**
- 两湖销区（湖北 + 湖南）
- 湖北销区
- 湖南销区

**预期查询结果（仅显示湖北和湖南的客户）：**

| 排名 | 客户名称 | 销区 | 超资信额（元） |
|------|---------|------|---------------|
| 1 | 丰沃达医药物流湖南有限公司 | 湖南 | 1,382,932.00 |
| 2 | 湖南和盛医药有限公司 | 湖南 | 34,733.40 |
| 3 | 国药控股湖北有限公司 | 湖北 | 16,983.20 |
| 4 | 湖北诚为上医药有限公司 | 湖北 | 78.90 |

## 五、权限控制测试

### 测试步骤

1. **配置权限控制**
   ```env
   ENABLE_ROLE_PERMISSION=true
   PERMISSION_TABLES=cw_ai_customer_receivables_analysis
   PERMISSION_FIELD=sales_region  # 需要改为 sales_region
   ```

2. **调用 MCP 工具时传入 user_id**
   ```python
   result = await mysql_query(
       query="SELECT customer_name, sales_region, over_credit_amount FROM cw_ai_customer_receivables_analysis WHERE year = 2025 AND month = 9 AND over_credit_amount > 0 ORDER BY over_credit_amount DESC LIMIT 10",
       user_id="01201811074"
   )
   ```

3. **预期权限过滤 SQL**
   ```sql
   SELECT customer_name, sales_region, over_credit_amount 
   FROM cw_ai_customer_receivables_analysis 
   WHERE year = 2025 
     AND month = 9 
     AND over_credit_amount > 0 
     AND (
       sales_region IN (
         SELECT REPLACE(EXTEND1, 'RX', '') 
         FROM fr_user_role 
         WHERE USERNAME = '01201811074'
       )
       OR '01201811074' NOT IN (SELECT USERNAME FROM fr_user_role)
     )
   ORDER BY over_credit_amount DESC 
   LIMIT 10
   ```

### 需要解决的问题

1. **权限字段配置**
   - 将 `PERMISSION_FIELD` 从 `gssq` 改为 `sales_region`

2. **权限值格式匹配**
   - 方案1：调整权限值格式，去掉 `Y` 和 `销区`，只保留 `湖北`、`湖南`、`两湖`
   - 方案2：调整权限过滤逻辑，支持模糊匹配或格式转换

3. **权限表配置**
   - 在 `PERMISSION_TABLES` 中添加 `cw_ai_customer_receivables_analysis`

## 六、建议的配置方案

### 方案1：使用 sales_region 字段（推荐）

```env
# 启用角色权限控制
ENABLE_ROLE_PERMISSION=true

# 配置需要权限过滤的表
PERMISSION_TABLES=cw_ai_customer_receivables_analysis

# 权限字段改为 sales_region
PERMISSION_FIELD=sales_region

# 用户角色表配置
USER_ROLE_TABLE=fr_user_role
USER_ROLE_USERNAME_FIELD=username
USER_ROLE_EXTEND_FIELD=extend1
USER_ROLE_PREFIX=RX
```

**权限值处理：**
需要调整权限值格式，将 `Y两湖RX销区` 转换为 `两湖` 或 `湖北,湖南`，或者调整权限过滤逻辑支持格式转换。

### 方案2：添加 gssq 字段到表

如果业务需要，可以在 `cw_ai_customer_receivables_analysis` 表中添加 `gssq` 字段，存储权限标识。

## 七、总结

1. ✅ **表结构已确认**：`cw_ai_customer_receivables_analysis` 表包含所需字段
2. ✅ **取数逻辑已明确**：按当前年月、超资信额排序取前10名
3. ⚠️ **权限控制需要配置**：
   - 权限字段需要改为 `sales_region`
   - 权限值格式需要调整或权限过滤逻辑需要支持格式转换
   - 权限表需要配置

4. **下一步操作**：
   - 配置 `.env` 文件
   - 调整权限值格式或权限过滤逻辑
   - 测试权限控制是否生效

